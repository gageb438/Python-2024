# the rectangle module contains functions related
# to rectangle calculations

def area(width, length):
    # area accepts integers for width and length
    # it calculates and returns the area of a rectangle
    
    # finds and returns the area
    return width * length

def perimeter(width, length):
    # area accepts integers for width and length
    # it calculates and returns the perimeter of a rectangle
    
    # finds and returns the perimeter
    return (width * 2) + (length * 2)